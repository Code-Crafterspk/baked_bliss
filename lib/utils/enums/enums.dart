enum OrderStatus {
  pending,
  confirmed,
  rejected,
  delivered,
  cancelled,
}

enum AuthMode {
  google,
  facebook,
  email,
  instagram,
  unknown,
}

enum SnackBarType {
  success,
  error,
  warning,
}
